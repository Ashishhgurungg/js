// Create a pure function to add tax

function addTax(amount, tax){
    return amount + tax;
}

console.log(addTax(1,2));

// you can also add the tax as percent as well but i have only added the tax amount directly